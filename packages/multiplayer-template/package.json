{
  "name": "@mavonengine/multiplayer-template",
  "version": "0.0.2",
  "workspaces": [
    "client",
    "server"
  ],
  "license": "MIT",
  "scripts": {
    "dev": "concurrently --kill-others -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "concurrently \"npm run build --prefix server\" \"npm run build --prefix client\"",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@mavonengine/core": "0.0.14"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^8.0.0",
    "@mavonengine/editor": "0.0.7",
    "@types/three": "^0.176.0",
    "concurrently": "^9.1.2",
    "eslint": "^10.1.0",
    "tsup": "^8.5.1",
    "typescript": "5.9.2"
  }
}
